## Managed by Salt Stack. Do NOT edit Manually!

# Running as Sandalone ("masterless") Minion.
# Note that the salt-minion daemon doesn't need to run
file_client: local
master: localhost

# Also, make sure init do not start the service
# under systemd, check for /etc/init/salt-minion.override
# containing "manual"
